import { Theme, withStyles, createStyles } from "@material-ui/core/styles";

const styles = (theme: Theme) =>
  createStyles({
    root: {
      width: "100%",
    },
 })   
 
 const AppBarWithSearchAndIcons = (props: any) = {
     return {
         ...
     }
 }
 
 export default withStyles(styles)(AppBarWithSearchAndIcons);
